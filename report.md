I've learnt how to setup a repository on GitHub.
I've also learnt the GitHub commands, that is,'git init'(always initialise git when using it),'git add'(this adds or stages files to your folder),'git push origin main(main-you can replace the branch and you do this after adding"git add .") and 'git switch "branch-name" and 'git commit -m "(message)"(i commit after add files)
